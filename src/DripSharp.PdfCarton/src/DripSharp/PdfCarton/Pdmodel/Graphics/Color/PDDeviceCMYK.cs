// <auto-generated />
// Mechanically translated from: org/apache/pdfbox/pdmodel/graphics/color/PDDeviceCMYK.java
// Upstream repository: https://github.com/apache/pdfbox.git
// Upstream revision: 9286e47d89d6877005c9d2d0f2fd38793a62519a
// Translator: DripSharp 0.1.0
// IMPORTANT: This mechanically translated derivative has been changed from the upstream source.
// Applicable upstream notices: see NOTICE.txt.
#nullable disable
namespace DripSharp.PdfCarton.Pdmodel.Graphics.Color;

public class PDDeviceCMYK : global::DripSharp.PdfCarton.Pdmodel.Graphics.Color.PDDeviceColorSpace {
  public static global::DripSharp.PdfCarton.Pdmodel.Graphics.Color.PDDeviceCMYK Instance = null!;

  static PDDeviceCMYK() { {
      global::DripSharp.PdfCarton.Pdmodel.Graphics.Color.PDDeviceCMYK.Instance
        = new global::DripSharp.PdfCarton.Pdmodel.Graphics.Color.PDDeviceCMYK();
    }
  }

  private readonly global::DripSharp.PdfCarton.Pdmodel.Graphics.Color.PDColor initialColor;

  private global::DripSharp.Runtime.JavaIccColorSpace awtColorSpace = null!;

  private volatile bool initDone = false;

  private bool usePureJavaCMYKConversion = false;

  protected internal PDDeviceCMYK() {
    this.initialColor
      = new global::DripSharp.PdfCarton.Pdmodel.Graphics.Color.PDColor(new float[] { 0, 0, 0, 1 },
      this);
  }

  protected internal virtual void Init() {
    if (this.initDone) {
      return;
    }
    lock (this) {
      if (this.initDone) {
        return;
      }
      global::DripSharp.Runtime.JavaIccProfile iccProfile = this.GetICCProfile();
      if ((iccProfile == default!)) {
        throw new global::System.IO.IOException("Default CMYK color profile could not be loaded");
      }
      this.awtColorSpace = new global::DripSharp.Runtime.JavaIccColorSpace(iccProfile);
      this.awtColorSpace.ToRgb(new float[] { 0, 0, 0, 0 });
      this.usePureJavaCMYKConversion
        = (global::DripSharp.Runtime.JavaCompat.GetProperty("org.apache.pdfbox.rendering.UsePureJavaCMYKConversion")
        != default!);
      this.initDone = true;
    }
  }

  protected internal virtual global::DripSharp.Runtime.JavaIccProfile GetICCProfile() {
    string resourceName = "/org/apache/pdfbox/resources/icc/CGATS001Compat-v2-micro.icc";
    global::System.IO.Stream resourceAsStream
      = global::DripSharp.Runtime.JavaCompat.ClassGetResourceAsStream(typeof(global::DripSharp.PdfCarton.Pdmodel.Graphics.Color.PDDeviceCMYK),
      resourceName);
    if ((resourceAsStream == default!)) {
      throw new global::System.IO.IOException(global::DripSharp.Runtime.JavaCompat.Concat(global::DripSharp.Runtime.JavaCompat.Concat("resource '",
        resourceName), "' not found"));
    }
    using (global::System.IO.Stream @is = new global::System.IO.BufferedStream(resourceAsStream)) {
      return global::DripSharp.Runtime.PdfCartonFontCompat.GetIccProfile(@is);
    }
  }

  public override string GetName() {
    return global::DripSharp.PdfCarton.Cos.COSName.Devicecmyk.GetName();
  }

  public override int GetNumberOfComponents() {
    return 4;
  }

  public override float[] GetDefaultDecode(int bitsPerComponent) {
    return new float[] { 0, 1, 0, 1, 0, 1, 0, 1 };
  }

  public override global::DripSharp.PdfCarton.Pdmodel.Graphics.Color.PDColor GetInitialColor() {
    return this.initialColor;
  }

  public override float[] ToRGB(float[] value) {
    this.Init();
    return this.awtColorSpace.ToRgb(value);
  }

  public override global::SkiaSharp.SKBitmap ToRawImage(global::DripSharp.Runtime.JavaRaster raster) {
    return default!;
  }

  public override global::SkiaSharp.SKBitmap ToRGBImage(global::DripSharp.Runtime.JavaRaster raster) {
    this.Init();
    return this.ToRGBImageAWT(raster, this.awtColorSpace);
  }

  protected internal override global::SkiaSharp.SKBitmap ToRGBImageAWT(global::DripSharp.Runtime.JavaRaster raster,
    global::DripSharp.Runtime.JavaColorSpace colorSpace) {
    if (this.usePureJavaCMYKConversion) {
      global::SkiaSharp.SKBitmap dest
        = global::DripSharp.Runtime.PdfCartonFontCompat.CreateBitmap(raster.Width, raster.Height,
        global::DripSharp.Runtime.PdfCartonFontCompat.TYPE_INT_RGB);
      global::DripSharp.Runtime.JavaColorSpace destCS
        = global::DripSharp.Runtime.PdfCartonFontCompat.GetColorModel(dest).ColorSpace;
      global::DripSharp.Runtime.JavaRaster destRaster
        = global::DripSharp.Runtime.PdfCartonFontCompat.GetRaster(dest);
      float[] srcValues = new float[4];
      float[] lastValues = new float[] { -1.0F, -1.0F, -1.0F, -1.0F };
      float[] destValues = new float[3];
      int startX = raster.MinX;
      int startY = raster.MinY;
      int endX = (raster.Width + startX);
      int endY = (raster.Height + startY);
      for (int x = startX; (x < endX); x++) {
        for (int y = startY; (y < endY); y++) {
          raster.GetPixel(x, y, srcValues);
          if (!global::DripSharp.Runtime.JavaCompat.ArrayEquals(lastValues, srcValues)) {
            lastValues[0] = srcValues[0];
            srcValues[0] = ((float)(srcValues[0]) / (float)255.0F);
            lastValues[1] = srcValues[1];
            srcValues[1] = ((float)(srcValues[1]) / (float)255.0F);
            lastValues[2] = srcValues[2];
            srcValues[2] = ((float)(srcValues[2]) / (float)255.0F);
            lastValues[3] = srcValues[3];
            srcValues[3] = ((float)(srcValues[3]) / (float)255.0F);
            destValues = destCS.FromCieXyz(colorSpace.ToCieXyz(srcValues));
            for (int k = 0; (k < destValues.Length); k++) {
              destValues[k] = (destValues[k] * 255.0F);
            }
          }
          destRaster.SetPixel(x, y, destValues);
        }
      }
      return dest;
    } else {
      return base.ToRGBImageAWT(raster, colorSpace);
    }
  }
}
