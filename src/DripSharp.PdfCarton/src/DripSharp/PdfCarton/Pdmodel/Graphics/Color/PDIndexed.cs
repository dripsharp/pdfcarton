// <auto-generated />
// Mechanically translated from: org/apache/pdfbox/pdmodel/graphics/color/PDIndexed.java
// Upstream repository: https://github.com/apache/pdfbox.git
// Upstream revision: 9286e47d89d6877005c9d2d0f2fd38793a62519a
// Translator: DripSharp 0.1.0
// IMPORTANT: This mechanically translated derivative has been changed from the upstream source.
// Applicable upstream notices: see NOTICE.txt.
#nullable disable
namespace DripSharp.PdfCarton.Pdmodel.Graphics.Color;

public sealed class PDIndexed
: global::DripSharp.PdfCarton.Pdmodel.Graphics.Color.PDSpecialColorSpace {
  private readonly global::DripSharp.PdfCarton.Pdmodel.Graphics.Color.PDColor initialColor;

  private global::DripSharp.PdfCarton.Pdmodel.Graphics.Color.PDColorSpace baseColorSpace = default!;

  private sbyte[] lookupData = null!;

  private float[][] colorTable = null!;

  private int actualMaxIndex = default;

  private int[][] rgbColorTable = null!;

  public PDIndexed() {
    this.initialColor
      = new global::DripSharp.PdfCarton.Pdmodel.Graphics.Color.PDColor(new float[] { 0 }, this);

    base.Array = new global::DripSharp.PdfCarton.Cos.COSArray();
    base.Array.Add(global::DripSharp.PdfCarton.Cos.COSName.Indexed);
    base.Array.Add(global::DripSharp.PdfCarton.Cos.COSName.Devicergb);
    base.Array.Add(global::DripSharp.PdfCarton.Cos.COSInteger.Get((long)(255)));
    base.Array.Add(global::DripSharp.PdfCarton.Cos.COSNull.Null);
  }

  public PDIndexed(global::DripSharp.PdfCarton.Cos.COSArray indexedArray) : this(indexedArray,
    (global::DripSharp.PdfCarton.Pdmodel.PDResources)default!) {

  }

  public PDIndexed(global::DripSharp.PdfCarton.Cos.COSArray indexedArray,
    global::DripSharp.PdfCarton.Pdmodel.PDResources resources) {
    this.initialColor
      = new global::DripSharp.PdfCarton.Pdmodel.Graphics.Color.PDColor(new float[] { 0 }, this);

    base.Array = indexedArray;
    this.baseColorSpace
      = global::DripSharp.PdfCarton.Pdmodel.Graphics.Color.PDColorSpace.Create(base.Array.Get(1),
      resources);
    this.readColorTable();
    this.initRgbColorTable();
  }

  public static global::DripSharp.PdfCarton.Pdmodel.Graphics.Color.PDIndexed Create(global::DripSharp.PdfCarton.Pdmodel.Graphics.Color.PDColorSpace @base,
    int hival, sbyte[] lookupData) {
    if ((@base == default!)) {
      throw new global::System.ArgumentException("base must not be null");
    }
    if ((lookupData == default!)) {
      throw new global::System.ArgumentException("lookupData must not be null");
    }
    if (((hival < 0) || (hival > 255))) {
      throw new global::System.ArgumentException(" hival has to be a positive value <= 255");
    }
    int expected = ((hival + 1) * @base.GetNumberOfComponents());
    if ((lookupData.Length < expected)) {
      throw new global::System.ArgumentException(global::DripSharp.Runtime.JavaCompat.Concat(global::DripSharp.Runtime.JavaCompat.Concat(global::DripSharp.Runtime.JavaCompat.Concat("lookupData too short: expected at least ",
        expected), " bytes ((hival+1) * components), got "), lookupData.Length));
    }
    global::DripSharp.PdfCarton.Pdmodel.Graphics.Color.PDIndexed pdIndexed
      = new global::DripSharp.PdfCarton.Pdmodel.Graphics.Color.PDIndexed();
    pdIndexed.baseColorSpace = @base;
    pdIndexed.Array.Set(1, @base.GetCOSObject());
    pdIndexed.Array.Set(2, hival);
    pdIndexed.lookupData = global::DripSharp.Runtime.JavaCompat.CopyOf<sbyte>(lookupData,
      lookupData.Length);
    global::DripSharp.PdfCarton.Cos.COSString cosLookupData
      = new global::DripSharp.PdfCarton.Cos.COSString(pdIndexed.lookupData, true);
    pdIndexed.Array.Set(3, cosLookupData);
    pdIndexed.readColorTable();
    pdIndexed.initRgbColorTable();
    return pdIndexed;
  }

  public override string GetName() {
    return global::DripSharp.PdfCarton.Cos.COSName.Indexed.GetName();
  }

  public override int GetNumberOfComponents() {
    return 1;
  }

  public override float[] GetDefaultDecode(int bitsPerComponent) {
    return new float[] { 0, ((float)(global::System.Math.Pow((double)(2),
        (double)(bitsPerComponent))) - 1) };
  }

  public override global::DripSharp.PdfCarton.Pdmodel.Graphics.Color.PDColor GetInitialColor() {
    return this.initialColor;
  }

  private void initRgbColorTable() {
    int numBaseComponents = this.baseColorSpace.GetNumberOfComponents();
    global::DripSharp.Runtime.JavaRaster baseRaster;
    try {
      baseRaster
        = global::DripSharp.Runtime.PdfCartonFontCompat.CreateBandedRaster(global::DripSharp.Runtime.PdfCartonFontCompat.DATA_BUFFER_TYPE_BYTE,
        (this.actualMaxIndex + 1), 1, numBaseComponents, new global::DripSharp.Runtime.JavaPoint(0,
        0));
    } catch (global::System.ArgumentException ex) {
      throw new global::System.IO.IOException(null, ex);
    }
    int[] @base = new int[numBaseComponents];
    for (int i__192_18 = 0, n__192_25 = this.actualMaxIndex; (i__192_18 <= n__192_25);
      i__192_18++) {
      for (int c = 0; (c < numBaseComponents); c++) {
        @base[c] = (int)((this.colorTable[i__192_18][c] * 255.0F));
      }
      baseRaster.SetPixel(i__192_18, 0, @base);
    }
    global::SkiaSharp.SKBitmap rgbImage = this.baseColorSpace.ToRGBImage(baseRaster);
    global::DripSharp.Runtime.JavaRaster rgbRaster
      = global::DripSharp.Runtime.PdfCartonFontCompat.GetRaster(rgbImage);
    this.rgbColorTable
      = global::DripSharp.Runtime.JavaCompat.NewJaggedArray<int>((this.actualMaxIndex + 1), 3);
    int[] nil = default!;
    for (int i__209_18 = 0, n__209_25 = this.actualMaxIndex; (i__209_18 <= n__209_25);
      i__209_18++) {
      this.rgbColorTable[i__209_18] = rgbRaster.GetPixel(i__209_18, 0, nil!);
    }
  }

  public override float[] ToRGB(float[] value) {
    if ((value.Length != 1)) {
      throw new global::System.ArgumentException("Indexed color spaces must have one color value");
    }
    int index = global::DripSharp.Runtime.JavaCompat.MathRoundFloat(value[0]);
    index = global::System.Math.Max(index, 0);
    index = global::System.Math.Min(index, this.actualMaxIndex);
    int[] rgb = this.rgbColorTable[index];
    return new float[] { (rgb[0] / (float)255.0F), (rgb[1] / (float)255.0F), (rgb[2]
        / (float)255.0F) };
  }

  public override global::SkiaSharp.SKBitmap ToRGBImage(global::DripSharp.Runtime.JavaRaster raster) {
    int width = raster.Width;
    int height = raster.Height;
    global::SkiaSharp.SKBitmap rgbImage
      = global::DripSharp.Runtime.PdfCartonFontCompat.CreateBitmap(width, height,
      global::DripSharp.Runtime.PdfCartonFontCompat.TYPE_INT_RGB);
    global::DripSharp.Runtime.JavaRaster rgbRaster
      = global::DripSharp.Runtime.PdfCartonFontCompat.GetRaster(rgbImage);
    int[] src = new int[width];
    for (int y = 0; (y < height); y++) {
      raster.GetPixels(0, y, width, 1, src);
      for (int x = 0; (x < width); x++) {
        int index = global::System.Math.Min(src[x], this.actualMaxIndex);
        rgbRaster.SetPixel(x, y, this.rgbColorTable[index]);
      }
    }
    return rgbImage;
  }

  public override global::SkiaSharp.SKBitmap ToRawImage(global::DripSharp.Runtime.JavaRaster raster) {
    if (((this.baseColorSpace is global::DripSharp.PdfCarton.Pdmodel.Graphics.Color.PDICCBased)
      && ((global::DripSharp.PdfCarton.Pdmodel.Graphics.Color.PDICCBased)(this.baseColorSpace!)).IsSRGB())) {
      sbyte[] r = new sbyte[this.colorTable.Length];
      sbyte[] g = new sbyte[this.colorTable.Length];
      sbyte[] b = new sbyte[this.colorTable.Length];
      for (int i = 0; (i < this.colorTable.Length); i++) {
        r[i] = unchecked((sbyte)(unchecked((sbyte)(((int)((this.colorTable[i][0] * 255)) & 255)))));
        g[i] = unchecked((sbyte)(unchecked((sbyte)(((int)((this.colorTable[i][1] * 255)) & 255)))));
        b[i] = unchecked((sbyte)(unchecked((sbyte)(((int)((this.colorTable[i][2] * 255)) & 255)))));
      }
      global::DripSharp.Runtime.JavaColorModel colorModel
        = global::DripSharp.Runtime.PdfCartonFontCompat.IndexColorModel(8, this.colorTable.Length,
        r, g, b);
      return global::DripSharp.Runtime.PdfCartonFontCompat.CreateImage(colorModel, raster, false,
        (global::DripSharp.Runtime.JavaHashtable<object, object>)default!);
    }
    return default!;
  }

  public global::DripSharp.PdfCarton.Pdmodel.Graphics.Color.PDColorSpace GetBaseColorSpace() {
    return this.baseColorSpace;
  }

  private int getHival() {
    return ((global::DripSharp.PdfCarton.Cos.COSNumber)(base.Array.GetObject(2)!)).IntValue();
  }

  private void readLookupData() {
    if ((this.lookupData == default!)) {
      global::DripSharp.PdfCarton.Cos.COSBase lookupTable = base.Array.GetObject(3);
      if ((lookupTable is global::DripSharp.PdfCarton.Cos.COSString)) {
        this.lookupData = ((global::DripSharp.PdfCarton.Cos.COSString)(lookupTable!)).GetBytes();
      } else {
        if ((lookupTable is global::DripSharp.PdfCarton.Cos.COSStream)) {
          this.lookupData
            = new global::DripSharp.PdfCarton.Pdmodel.Common.PDStream((global::DripSharp.PdfCarton.Cos.COSStream)(lookupTable!)).ToByteArray();
        } else {
          if ((lookupTable == default!)) {
            this.lookupData = new sbyte[0];
          } else {
            throw new global::System.IO.IOException(global::DripSharp.Runtime.JavaCompat.Concat("Error: Unknown type for lookup table ",
              lookupTable));
          }
        }
      }
    }
  }

  private void readColorTable() {
    this.readLookupData();
    int maxIndex = global::System.Math.Min(this.getHival(), 255);
    int numComponents = this.baseColorSpace.GetNumberOfComponents();
    if (((this.lookupData.Length / numComponents) < (maxIndex + 1))) {
      maxIndex = ((this.lookupData.Length / numComponents) - 1);
    }
    this.actualMaxIndex = maxIndex;
    this.colorTable = global::DripSharp.Runtime.JavaCompat.NewJaggedArray<float>((maxIndex + 1),
      numComponents);
    for (int i = 0, offset = 0; (i <= maxIndex); i++) {
      for (int c = 0; (c < numComponents); c++) {
        this.colorTable[i][c] = ((this.lookupData[offset] & 255) / (float)255.0F);
        offset++;
      }
    }
  }

  public void SetBaseColorSpace(global::DripSharp.PdfCarton.Pdmodel.Graphics.Color.PDColorSpace @base) {
    base.Array.Set(1, @base.GetCOSObject());
    this.baseColorSpace = @base;
  }

  public void SetHighValue(int high) {
    base.Array.Set(2, high);
  }

  public override string ToString() {
    return global::DripSharp.Runtime.JavaCompat.Concat(global::DripSharp.Runtime.JavaCompat.Concat(global::DripSharp.Runtime.JavaCompat.Concat(global::DripSharp.Runtime.JavaCompat.Concat(global::DripSharp.Runtime.JavaCompat.Concat(global::DripSharp.Runtime.JavaCompat.Concat(global::DripSharp.Runtime.JavaCompat.Concat(global::DripSharp.Runtime.JavaCompat.Concat("Indexed{base:",
      this.baseColorSpace), " "), "hival:"), this.getHival()), " "), "lookup:("),
      this.colorTable.Length), " entries)}");
  }
}
