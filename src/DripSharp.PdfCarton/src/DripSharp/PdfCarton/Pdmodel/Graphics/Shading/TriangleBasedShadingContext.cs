// <auto-generated />
// Mechanically translated from: org/apache/pdfbox/pdmodel/graphics/shading/TriangleBasedShadingContext.java
// Upstream repository: https://github.com/apache/pdfbox.git
// Upstream revision: 9286e47d89d6877005c9d2d0f2fd38793a62519a
// Translator: DripSharp 0.1.0
// IMPORTANT: This mechanically translated derivative has been changed from the upstream source.
// Applicable upstream notices: see NOTICE.txt.
#nullable disable
namespace DripSharp.PdfCarton.Pdmodel.Graphics.Shading;

internal abstract class TriangleBasedShadingContext
: global::DripSharp.PdfCarton.Pdmodel.Graphics.Shading.ShadingContext {
  private int[][] pixelTableArray = null!;

  private int xOffset = 0;

  private int yOffset = 0;

  internal TriangleBasedShadingContext(global::DripSharp.PdfCarton.Pdmodel.Graphics.Shading.PDShading shading,
    global::DripSharp.Runtime.JavaColorModel cm, global::SkiaSharp.SKMatrix xform,
    global::DripSharp.PdfCarton.Util.Matrix matrix) : base(shading, cm, xform, matrix) {

  }

  protected internal void CreatePixelTable(global::SkiaSharp.SKRectI deviceBounds) {
    this.xOffset = -(deviceBounds.Left);
    this.yOffset = -(deviceBounds.Top);
    this.pixelTableArray = this.calcPixelTableArray(deviceBounds);
  }

  protected internal abstract global::System.Collections.Generic.IDictionary<global::DripSharp.Runtime.JavaPoint,
    int> calcPixelTable(global::SkiaSharp.SKRectI deviceBounds);

  protected internal abstract int[][] calcPixelTableArray(global::SkiaSharp.SKRectI deviceBounds);

  protected internal virtual void CalcPixelTable(global::System.Collections.Generic.IList<global::DripSharp.PdfCarton.Pdmodel.Graphics.Shading.ShadedTriangle> triangleList,
    global::System.Collections.Generic.IDictionary<global::DripSharp.Runtime.JavaPoint, int> map,
    global::SkiaSharp.SKRectI deviceBounds) {}

  protected internal virtual int[][] CalcPixelTable(global::System.Collections.Generic.IList<global::DripSharp.PdfCarton.Pdmodel.Graphics.Shading.ShadedTriangle> triangleList,
    int[][] array, global::SkiaSharp.SKRectI deviceBounds) {
    foreach (global::DripSharp.PdfCarton.Pdmodel.Graphics.Shading.ShadedTriangle tri in triangleList) {
      int degree = tri.GetDeg();
      if ((degree == 2)) {
        this.addLinePoints(tri.GetLine(), array);
      } else {
        int[] boundary = tri.GetBoundary();
        boundary[0] = global::System.Math.Max(boundary[0], deviceBounds.Left);
        boundary[1] = global::System.Math.Min(boundary[1], (deviceBounds.Left
          + deviceBounds.Width));
        boundary[2] = global::System.Math.Max(boundary[2], deviceBounds.Top);
        boundary[3] = global::System.Math.Min(boundary[3], (deviceBounds.Top
          + deviceBounds.Height));
        for (int x = boundary[0]; (x <= boundary[1]); x++) {
          for (int y = boundary[2]; (y <= boundary[3]); y++) {
            global::DripSharp.Runtime.JavaPoint p = new global::DripSharp.Runtime.JavaPoint(x, y);
            if (tri.Contains(p)) {
              this.addValueToArray(p, this.evalFunctionAndConvertToRGB(tri.CalcColor(p)), array);
            }
          }
        }
        global::DripSharp.Runtime.JavaPoint p0
          = new global::DripSharp.Runtime.JavaPoint((int)((int)global::DripSharp.Runtime.JavaCompat.MathRound(tri.Corner[0].X)),
          (int)((int)global::DripSharp.Runtime.JavaCompat.MathRound(tri.Corner[0].Y)));
        global::DripSharp.Runtime.JavaPoint p1
          = new global::DripSharp.Runtime.JavaPoint((int)((int)global::DripSharp.Runtime.JavaCompat.MathRound(tri.Corner[1].X)),
          (int)((int)global::DripSharp.Runtime.JavaCompat.MathRound(tri.Corner[1].Y)));
        global::DripSharp.Runtime.JavaPoint p2
          = new global::DripSharp.Runtime.JavaPoint((int)((int)global::DripSharp.Runtime.JavaCompat.MathRound(tri.Corner[2].X)),
          (int)((int)global::DripSharp.Runtime.JavaCompat.MathRound(tri.Corner[2].Y)));
        this.addLinePoints(new global::DripSharp.PdfCarton.Pdmodel.Graphics.Shading.Line(p0, p1,
          tri.Color[0], tri.Color[1]), array);
        this.addLinePoints(new global::DripSharp.PdfCarton.Pdmodel.Graphics.Shading.Line(p1, p2,
          tri.Color[1], tri.Color[2]), array);
        this.addLinePoints(new global::DripSharp.PdfCarton.Pdmodel.Graphics.Shading.Line(p2, p0,
          tri.Color[2], tri.Color[0]), array);
      }
    }
    return array;
  }

  private void addLinePoints(global::DripSharp.PdfCarton.Pdmodel.Graphics.Shading.Line line,
    int[][] array) {
    foreach (global::DripSharp.Runtime.JavaPoint p in line.LinePoints) {
      this.addValueToArray(p, this.evalFunctionAndConvertToRGB(line.CalcColor(p)), array);
    }
  }

  private void addValueToArray(global::DripSharp.Runtime.JavaPoint p, int value, int[][] array) {
    int xIndex = (p.IntX + this.xOffset);
    int yIndex = (p.IntY + this.yOffset);
    if (((((xIndex < 0) || (yIndex < 0)) || (xIndex >= array.Length)) || (yIndex
      >= array[0].Length))) {
      return;
    }
    array[xIndex][yIndex] = value;
  }

  private int getValueFromArray(int x, int y) {
    int xIndex = (x + this.xOffset);
    int yIndex = (y + this.yOffset);
    if (((((xIndex < 0) || (yIndex < 0)) || (xIndex >= this.pixelTableArray.Length)) || (yIndex
      >= this.pixelTableArray[0].Length))) {
      return -1;
    }
    return this.pixelTableArray[xIndex][yIndex];
  }

  private int evalFunctionAndConvertToRGB(float[] values) {
    if ((this.getShading().GetFunction() != default!)) {
      values = this.getShading().EvalFunction(values);
    }
    return this.convertToRGB(values);
  }

  protected internal abstract bool isDataEmpty();

  public override global::DripSharp.Runtime.JavaRaster GetRaster(int x, int y, int w, int h) {
    global::DripSharp.Runtime.JavaRaster raster
      = this.GetColorModel().CreateCompatibleWritableRaster(w, h);
    int[] data = new int[((w * h) * 4)];
    if ((!(this.isDataEmpty()) || (this.getBackground() != default!))) {
      for (int row = 0; (row < h); row++) {
        for (int col = 0; (col < w); col++) {
          int value = this.getValueFromArray((x + col), (y + row));
          if ((value >= 0)) {
            int index = (((row * w) + col) * 4);
            data[index] = (value & 255);
            value >>= 8;
            data[(index + 1)] = (value & 255);
            value >>= 8;
            data[(index + 2)] = (value & 255);
            data[(index + 3)] = 255;
          }
        }
      }
    }
    raster.SetPixels(0, 0, w, h, data);
    return raster;
  }
}
